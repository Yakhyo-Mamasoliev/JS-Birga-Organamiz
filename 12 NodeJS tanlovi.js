// WEB(html, css, js) 

// JSni webdan tahsqarida ishlatish uchun NODEJS yaratilgan
// V8 va libuv degan enjinlar yordamida yaratilgan (both engines created using C++)

// Real time is the most important in Today`s webpages

// NESTJS is build via ExpresJS so learn expressJs first

// nvm nodeni turli xil versialarni install qilish uchun kerak
// cmd commands
// nvm --version => checks if nvm is installed before
// nvm list => shows installed nvm versions
//  nvm install 14.7.0 => installs exact 14.7.0 version
// nvm uninstall 14.7.0 => ininstalls